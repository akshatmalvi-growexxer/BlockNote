{
  "name": "api",
  "private": true,
  "version": "0.1.0",
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "build": "node -e \"console.log('API build is runtime-only for now')\"",
    "lint": "node --check src/server.js && node --check src/app.js && node --check src/config/env.js",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "start": "node src/server.js"
  }
}
